'use strict';

/**
 * sticker-submission service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sticker-submission.sticker-submission');
