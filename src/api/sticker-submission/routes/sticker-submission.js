'use strict';

/**
 * sticker-submission router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sticker-submission.sticker-submission');
